"use client";

// This module exists so the dynamic import in page.tsx resolves.
// Face comparison logic runs directly in page.tsx's runFaceComparison().
// This is a no-op placeholder.
export default function FaceMatchInline() {
  return null;
}
